

# Database configuration
spring.datasource.url=jdbc:postgresql://localhost:5555/restaurantdb
spring.datasource.username=postgres
spring.datasource.password=root
#spring.datasource.driver-class-name=org.postgresql.Driver

# JPA (Hibernate) Configuration
spring.jpa.hibernate.ddl-auto=update   
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Enable SQL logging (optional)
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

info.app.name=restaurant-Service
info.app.description=This is restaurant service dev environment.Git Version. keep on doing developers
info.app.version=1.0 Dev
